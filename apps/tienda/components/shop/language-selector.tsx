'use client';

/** v1: solo español en producción; selector oculto hasta migración [locale] completa. */
export function LanguageSelector({ className = '' }: { className?: string }) {
  return null;
}