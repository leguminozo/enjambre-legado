import { ResetPasswordForm } from '@/components/auth/reset-password-form';

export const metadata = {
  title: 'Nueva contraseña · La Obrera',
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}