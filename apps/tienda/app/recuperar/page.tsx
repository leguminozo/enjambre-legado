import { RecuperarForm } from '@/components/auth/recuperar-form';

export const metadata = {
  title: 'Recuperar contraseña · La Obrera',
};

export default function RecuperarPage() {
  return <RecuperarForm />;
}